# Challenges — status board

> Root index (`./challenges.md`). Scout runs **once** → ranked here cheapest-flag-first (top = solve first). **Rank is one-shot**; afterward only the `status` + `flag / last attempt` cells change. Full per-challenge detail stays under `challenges/<cat>/NN_<slug>/`.
> Updated: {{TIMESTAMP}}

| rank | challenge | cat | pts | effort | score | stack | status | flag / last attempt |
|------|-----------|-----|-----|--------|-------|-------|--------|---------------------|
{{ROWS}}

<!-- challenge: path `challenges/<cat>/NN_<slug>/` -->
<!-- status: queued / solving / solved / unsolved / blocked(prereq) / already_solved -->
<!-- flag / last attempt: grey{...} if solved; else link that folder's `solve/ATTEMPTS.md` (where the last agent try lives) -->
<!-- effort S/M/L · score 0-100 · stack e.g. crypto+sage, pwn+pwntools, web+browser, rev+ghidra -->
