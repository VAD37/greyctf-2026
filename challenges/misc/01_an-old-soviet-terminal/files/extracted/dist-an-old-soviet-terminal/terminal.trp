import timeout

exitAfterTimeout authority 2000 0 "";

let val tid = node (self ())
    val _ = send ( whereis ("@receiver", "receiver"), ("start", self()) )
    val (logService, analysisService) = receive [ hn (ls, as) => (ls, as) ]
in

let 

fun retriever () =   (* Should return flag as string *)
<<CODE>>

in
    send( whereis ("@receiver", "receiver"), (self(), retriever) );
    printString("Message: " ^ receive [hn flag => flag ])
end

end